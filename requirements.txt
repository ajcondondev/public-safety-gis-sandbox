# =============================================================================
# Python dependencies for the Concord-Manchester Public Safety GIS pipeline
# -----------------------------------------------------------------------------
# CORE (required): the pipeline (scripts 01-06) runs with ONLY these two.
#   pandas writes/reads tabular data; PyYAML reads the config file.
#   GeoJSON is written with the standard-library `json` module and proximity
#   analysis uses a pure-Python haversine function, so NO heavy geospatial
#   stack is needed to run the demo end-to-end.
# =============================================================================
pandas>=2.0
PyYAML>=6.0

# -----------------------------------------------------------------------------
# OPTIONAL (enhancements): install these for "real GIS" geometry handling.
# The scripts auto-detect GeoPandas/Shapely and use them if present (e.g. for
# true point-in-polygon hazard tests); otherwise they fall back to lightweight
# pure-Python approximations. Install with:  pip install geopandas shapely
# -----------------------------------------------------------------------------
# geopandas>=0.14
# shapely>=2.0

# -----------------------------------------------------------------------------
# OPTIONAL (notebook): to run notebooks/exploratory_analysis.ipynb
# -----------------------------------------------------------------------------
# jupyter
# matplotlib

# -----------------------------------------------------------------------------
# OPTIONAL (Esri / ArcGIS): NOT installable via pip on its own.
#   - arcpy ships with ArcGIS Pro (Python 3 conda environment).
#   - arcgis (ArcGIS API for Python) installs via: conda install -c esri arcgis
# The codebase does NOT require these; ArcPy hooks are clearly marked optional.
# -----------------------------------------------------------------------------
# arcpy        # provided by ArcGIS Pro
# arcgis       # ArcGIS API for Python (conda)
